import React from "react";

const Box = () => {
  const boxStyles = {
    width: "200px",
    height: "200px",
    backgroundColor: "pink",
    margin: "10px",
  };

  return <div style={boxStyles}></div>;
};

export default Box;
