import React from "react";

const SideNav = () => {
  const sideNavStyles = {
    height: "90vh",
    width: "30%",
    backgroundColor: "gray",
  };

  return (
    <div style={sideNavStyles}>
      <h1>hello</h1>
    </div>
  );
};

export default SideNav;
