import React from "react";

const SideNav = () => {
  const sideNavStyles = {
    height: "90vh",
    width: "30%",
    backgroundColor: "gray",
    position: "sticky",
    top: "10vh",
  };

  return <div style={sideNavStyles}></div>;
};

export default SideNav;
